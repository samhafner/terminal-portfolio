import { defineStore } from 'pinia'

export const useTerminalStore = defineStore('terminal', {
    state: () => ({
        componentIsProcessing: false,
    }),
    getters: {
    },
    actions: {
    },
})
